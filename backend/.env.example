# Database
DATABASE_URL=sqlite:///./app.db

# JWT Authentication
JWT_SECRET_KEY=change-me-to-a-random-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# File Storage
UPLOAD_DIR=./uploads

# OCR Engine: "easyocr" or "tesseract"
OCR_ENGINE=tesseract

# Email (SMTP) - Optional, notifications disabled if not set
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# OneDrive via Power Automate - Optional
POWER_AUTOMATE_WEBHOOK_URL=
ONEDRIVE_BASE_FOLDER=XpressTech

# Australian Business Register (ABN lookup) - Optional, lookup disabled if not set
# Free GUID: register at https://abr.business.gov.au/Tools/WebServices
ABR_GUID=

# Environment
ENVIRONMENT=development

# Tenant resolution. Production reads the tenant from the Host subdomain.
# On a host with no tenant subdomain (apex domain, www) set the tenant here:
# DEFAULT_TENANT_SLUG=default
#
# X-Tenant-Slug header / ?tenant= are honoured only when this is true.
# Defaults to true in development, false in production.
# ALLOW_TENANT_HEADER=false

# Frontend URL for referral links and emails
FRONTEND_URL=https://xpresstech.ai
